# Per-observation source holds

The original-lineage footnote guard now accumulates diagnostics rather than stopping
at the first unverified value. Both numeric comparison and source observation preview
use this shared guard. Preview entries expose observation ID, inherited open source
issue IDs, unresolved canonical note IDs and reasons. Original parent/column ownership,
unassigned page notes, unsupported/mismatched literals and unverified note sources stay
visible even when the observation already has another hold. No issue is resolved and
no source/coverage/numeric approval is granted. Native notes retain artifact-backed issue
IDs; they receive no fabricated canonical note IDs.

A new synthetic check first failed on the absent diagnostics function, then passed
with the existing numeric cases. It removes the observation's footnote text/ref and
confirms the original note and parent issue still appear. A complete verified unit
literal has no note hold; dropping its original ownership produces an owner hold.
The actual HTTP regression checks returned hold identity and unverified-source reason.
152 focused checks passed, with two existing dependency warnings, in 6.22s.

Actual LG Chem p97 API replay produced nine candidates, each carrying both applicable
issue IDs and source-issue/source-verification reasons. The envelope retains all three
published issues (the third is a geometry issue not selected by the inherited-source-ID guard). Wrong year and metric row bindings
return 422; revision 5, graph and job-record count remain unchanged. Private evidence:
`.local/note-review-integration/source-observation-holds-v1/`. Response digest:
`98adfbbfd0e52f4111ec27b486c4f3866f8f95a5b4b0ae5581b136f9d4460c5a`.

Orca native-ownership audit `task_ab077b4a2036` / `ctx_2cebae39584d` inspected LG's
original page, packet/checkpoint and revision 5. Biomass fragment f97 has no marker;
numbered f98–f103 have no supported target-marker matches, no separate markers and no
original footnote_of edges. The current whole-table ownership proposal has empty source
refs, while the model proposed a Scope-2 intensity row. Neither is authoritative proof.
Artifact: `522d0921f801387b8ec41f2cd6c05ed5b9ed677531a2ecae366c2719d0b658fc`;
packet: `2e430ecd53dfc4a1ba9dea9641ace583fd9044d22d6dd7300688b14023eda063`.
The audit worker settled, was released and its delivery acknowledged. LG remains a
negative ownership example; biomass prose would remain unsupported even if ownership
were established. No proximity or model proposal was promoted into proof.

All 18 installed-wheel report checkpoint replays preserved original graph hashes.
Scanning their pinned native fragments with the existing marker proposal guard found
no nonempty target set; this is not evidence that the reports contain no real links.
It identifies the need for a fresh original-backed positive marker/cross-reference
example before implementing native ownership acceptance. Private scan:
`.local/note-review-integration/source-holds-marker-wheel.json`.

Ruff lint/format (291 files), CI mypy (172 files), architecture checks and all four
Python package builds passed. No new dependency, model call or budget ledger mutation.
Model/AWS/deployment/browser-interaction checks were not_run in this slice. This is
review diagnostics, not the approved-view/claim-binding/numeric-receipt path still needed
for the service goal. Additive response rollback needs no database migration.

Independent Orca review `task_2b626f652a84` / `ctx_5137cf043aed` ran 152 numeric/API
checks, one native/conflicting-table check and three HTTP schema/hash probes. It found
an invalid selected-candidate index could raise after another hold, and a removed value
ref could hide a root-specific issue from diagnostics. Reproduced both in failing tests;
added type/range guards and retained parent-relation root IDs as issue seeds. A duplicate
original-note guard also preserves the prior rejection instead of deduplicating silently.
Final focused run: **158 passed, 2 warnings in 5.89s**. The reviewer settled, was
released and acknowledged; its review preceded these coordinator fixes.
The final LG replay retained all behavior above with current-code digest
`d0612f45adaa3ae98df0d08687abdcbbe716489e274ae4edcdf24ab8c263e644`;
private final evidence is `source-observation-holds-v2/`, leaving v1 unchanged.

To obtain a positive ownership example, scanned the final 55 pages of six additional/
existing energy-sector report PDFs locally for unit-label contexts. Eight text hits are
discovery candidates, not verified bindings. Rendered and visually inspected POSCO
Future M physical p139: the right-hand Scope 3 table has a raised 2) on 합계, followed
by note 2) below that same table. Other numbered notes elsewhere on the page demonstrate
why page-wide marker equality is insufficient. Its rounding prose remains unsupported
by the bounded condition grammar even if this ownership can be proven. Original PDF
SHA256 `65070d88297d4faf631a9545e6aa49b56ccae71531dda0248f69a3887ab779a8`;
private image/record: `.local/note-review-integration/original-marker-positive-v1/`.
This is the next source-bound native ownership implementation candidate, not an accepted
runtime link, new numeric unit alias or production verification.

After all fixes, full application suite: **2065 passed, 2 warnings in 139.70s**;
`.local/note-review-integration/source-holds-final-full.log`. Final documentation/
contract validation: 790 passed checks, 50 operations; this is not a performance score.
